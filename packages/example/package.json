{
  "private": true,
  "name": "example",
  "scripts": {
    "start": "wrangler pages dev static",
    "clean": "rm -rf dist",
    "build": "wrangler pages functions build --outfile=dist/index.js --sourcemap"
  },
  "dependencies": {
    "@suzulabo/pages-plugin-auth": "link:../.."
  }
}
