{
  "scripts": {
    "clean": "rm -rf dist",
    "build": "wrangler pages functions build --plugin --outfile=dist/index.js",
    "build:watch": "pnpm run build --watch"
  },
  "dependencies": {
    "jose": "^4.9.3"
  }
}
